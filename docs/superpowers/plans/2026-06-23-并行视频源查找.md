# 并行视频源查找 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「视频下载链接查找器」从"单源串行 + 手动切换"改成"3 路并行跨所有已启用源，每源出正片或 120s/404 失败即让槽，收集每源一条正片"。

**Architecture:** 方案 A —— 后端 `video_finder` 支持多窗口（per-site label + 事件打 siteId 标签），前端写调度器 composable 管队列/并发/超时/正片判定（复用现有 `analyzeHls` + 抽出的 `pickRealLink` 纯函数）。

**Tech Stack:** Rust + Tauri 2 + tokio（后端）；Vue 3 + TypeScript + Pinia + Vitest（前端）。包管理用 bun。

设计来源：`docs/superpowers/specs/2026-06-23-并行视频源查找-design.md`

## Global Constraints

- 包管理只用 `bun`，禁止 npm/yarn/pnpm。
- 编译验证：Rust 用 `cargo check`（在 `src-tauri/` 下），前端用 `bun run build`（含 vue-tsc）；单测用 `bunx vitest run <file>`。
- 所有对话、注释、commit message 使用中文。
- commit message 格式 `<类型>: <描述>`，类型 feat/fix/refactor/test/chore；结尾加 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。
- 并发硬上限 3 个 webview；单源默认预算 120 秒；正片判定 = HLS + 时长 ≥ 300 秒 + 优先 master/高分辨率（沿用现有）。
- 前后端通信唯一通道是 Tauri IPC；改 command 时同步改前端 `invoke` 调用处。

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/utils/pickRealLink.ts` | 正片判定纯函数（从 VideoLinkFinder 抽出） | 新建 |
| `src/utils/__tests__/pickRealLink.test.ts` | 上者单测 | 新建 |
| `src-tauri/src/settings/mod.rs` | 新增 `linkFinderConcurrency` / `linkFinderSourceTimeoutSecs` | 改 |
| `src/types/settings.ts` | 同上前端类型 + 默认值 | 改 |
| `src-tauri/src/resource_scrape/video_finder.rs` | per-site 窗口 + 事件打标签 + close(site)/close_all | 改 |
| `src-tauri/src/resource_scrape/link_probe.rs` | 用独立 probe label（不再借 VIDEO_FINDER_LABEL） | 改 |
| `src-tauri/src/resource_scrape/commands.rs` | close 命令带 site_id + 新增 close_all 命令 | 改 |
| `src-tauri/src/lib.rs` | 注册新/改命令 | 改 |
| `src/lib/tauri.ts` | findVideoLinks(code,site)/closeVideoFinder(site)/closeAllVideoFinders + 事件 payload 类型 | 改 |
| `src/composables/useParallelFinder.ts` | 调度器（队列/并发/超时/正片判定/事件分流） | 新建 |
| `src/composables/__tests__/useParallelFinder.test.ts` | 调度器纯逻辑单测 | 新建 |
| `src/components/VideoLinkFinder.vue` | UI 改造为每源一行 + 接调度器 | 改 |

---

## Task 1: 抽出 `pickRealLink` 正片判定纯函数（TDD）

把 `VideoLinkFinder.vue` 里 `realLink` 的内联逻辑抽成可单测的纯函数。本任务**仅新增文件**，不改组件（Task 6 再替换组件里的内联逻辑为调用它）。

**Files:**
- Create: `src/utils/pickRealLink.ts`
- Test: `src/utils/__tests__/pickRealLink.test.ts`

**Interfaces:**
- Consumes: 现有 `VideoLink` 类型（`src/lib/tauri.ts` 导出，含 `durationSecs?/height?/isMaster?` 等）。
- Produces: `pickRealLink(links: VideoLink[]): VideoLink | null`

- [ ] **Step 1: 写失败测试**

```ts
// src/utils/__tests__/pickRealLink.test.ts
import { describe, it, expect } from 'vitest'
import { pickRealLink } from '../pickRealLink'
import type { VideoLink } from '@/lib/tauri'

const mk = (p: Partial<VideoLink>): VideoLink => ({
  url: p.url ?? 'u', linkType: 'm3u8', isHls: true, resolution: null, ...p,
})

describe('pickRealLink', () => {
  it('无任何带时长链接 → null', () => {
    expect(pickRealLink([mk({ url: 'a' })])).toBeNull()
  })
  it('最长时长 < 300 秒（全是广告/片段）→ null', () => {
    expect(pickRealLink([mk({ url: 'a', durationSecs: 100 }), mk({ url: 'b', durationSecs: 250 })])).toBeNull()
  })
  it('时长 ≥ 300 秒 → 选出正片', () => {
    const r = pickRealLink([mk({ url: 'ad', durationSecs: 60 }), mk({ url: 'main', durationSecs: 3600 })])
    expect(r?.url).toBe('main')
  })
  it('同片多清晰度（时长接近）→ 优先 master', () => {
    const r = pickRealLink([
      mk({ url: 'v1080', durationSecs: 3600, height: 1080, isMaster: false }),
      mk({ url: 'master', durationSecs: 3600, height: 0, isMaster: true }),
    ])
    expect(r?.url).toBe('master')
  })
  it('都非 master → 选更高分辨率', () => {
    const r = pickRealLink([
      mk({ url: 'v720', durationSecs: 3600, height: 720 }),
      mk({ url: 'v1080', durationSecs: 3600, height: 1080 }),
    ])
    expect(r?.url).toBe('v1080')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bunx vitest run src/utils/__tests__/pickRealLink.test.ts`
Expected: FAIL（`pickRealLink` 不存在 / 模块找不到）

- [ ] **Step 3: 写实现（逐字搬现有 realLink 逻辑）**

```ts
// src/utils/pickRealLink.ts
import type { VideoLink } from '@/lib/tauri'

/**
 * 从一个源捕获到的链接里选出「真实正片」：
 * 时长最长（≥ 300 秒）的那一条；同片多清晰度时优先主列表(master)、再高分辨率、再长时长。
 * 与原 VideoLinkFinder.vue 的 realLink 逻辑保持一致。
 */
export function pickRealLink(links: VideoLink[]): VideoLink | null {
  const analyzed = links.filter((l) => (l.durationSecs ?? 0) > 0)
  if (!analyzed.length) return null
  const maxDur = Math.max(...analyzed.map((l) => l.durationSecs ?? 0))
  if (maxDur < 300) return null
  const candidates = analyzed.filter((l) => (l.durationSecs ?? 0) >= maxDur * 0.9)
  candidates.sort((a, b) => {
    if (!!b.isMaster !== !!a.isMaster) return (b.isMaster ? 1 : 0) - (a.isMaster ? 1 : 0)
    const hDiff = (b.height ?? 0) - (a.height ?? 0)
    if (hDiff !== 0) return hDiff
    return (b.durationSecs ?? 0) - (a.durationSecs ?? 0)
  })
  return candidates[0] ?? null
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bunx vitest run src/utils/__tests__/pickRealLink.test.ts`
Expected: PASS（5 passed）

- [ ] **Step 5: 提交**

```bash
git add src/utils/pickRealLink.ts src/utils/__tests__/pickRealLink.test.ts
git commit -m "test: 抽出 pickRealLink 正片判定纯函数并单测

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 新增两个设置项（并发数 / 单源预算）

**Files:**
- Modify: `src-tauri/src/settings/mod.rs`（`ScrapeSettings` 结构 + 默认 + `normalize_scrape_settings` clamp）
- Modify: `src/types/settings.ts`（接口 + 默认值）

**Interfaces:**
- Produces: 设置字段 `scrape.linkFinderConcurrency: u32`（默认 3，clamp 1..=3）、`scrape.linkFinderSourceTimeoutSecs: u32`（默认 120，clamp 30..=600）。前端同名 camelCase。

- [ ] **Step 1: 后端加字段 + 默认 + clamp**

在 `src-tauri/src/settings/mod.rs` 的 `ScrapeSettings` 里，仿照现有 `max_webview_windows` 字段加：

```rust
    #[serde(rename = "linkFinderConcurrency", default = "default_link_finder_concurrency")]
    pub link_finder_concurrency: u32,
    #[serde(rename = "linkFinderSourceTimeoutSecs", default = "default_link_finder_source_timeout")]
    pub link_finder_source_timeout_secs: u32,
```

在文件内合适处加默认函数（仿现有 default 函数风格）：

```rust
fn default_link_finder_concurrency() -> u32 { 3 }
fn default_link_finder_source_timeout() -> u32 { 120 }
```

在 `Default for ScrapeSettings`（或对应默认构造处）补这两字段的默认值 `3` / `120`；在 `normalize_scrape_settings` 里加：

```rust
    scrape.link_finder_concurrency = scrape.link_finder_concurrency.clamp(1, 3);
    scrape.link_finder_source_timeout_secs = scrape.link_finder_source_timeout_secs.clamp(30, 600);
```

> 先 `grep -n "max_webview_windows" src/settings/mod.rs` 找到这三处（字段定义、Default、normalize）照着加，保证不漏。

- [ ] **Step 2: 后端编译**

Run: `cd src-tauri && cargo check`
Expected: Finished，无错误

- [ ] **Step 3: 前端类型 + 默认**

在 `src/types/settings.ts` 的 scrape 设置接口里加：

```ts
  linkFinderConcurrency: number // 并行查找源的最大并发数（1-3）
  linkFinderSourceTimeoutSecs: number // 单个源最长查找秒数（无正片即放弃）
```

并在该文件的默认设置对象（含 `webviewFallbackEnabled: false` 那个）补：

```ts
    linkFinderConcurrency: 3,
    linkFinderSourceTimeoutSecs: 120,
```

- [ ] **Step 4: 前端类型检查**

Run: `bunx vue-tsc --noEmit`
Expected: 无输出（通过）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/settings/mod.rs src/types/settings.ts
git commit -m "feat: 新增并行查找并发数与单源预算设置项

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: 后端 video_finder 改 per-site 多窗口 + 事件打标签

**Files:**
- Modify: `src-tauri/src/resource_scrape/video_finder.rs`
- Modify: `src-tauri/src/resource_scrape/link_probe.rs`
- Modify: `src-tauri/src/resource_scrape/commands.rs`
- Modify: `src-tauri/src/lib.rs`

**Interfaces:**
- Produces（事件 payload，前端 Task 4 依赖）：
  - `video-finder-link` → `{ "site": string, "url": string }`
  - `video-finder-page-state` → `{ "site": string, "state": "not-found" }`
  - `video-finder-cf-state` → 现有结构基础上加 `"site": string`
- Produces（command）：
  - `find_video_links(code, siteId)`（现有，行为不变，内部用 per-site label）
  - `close_video_finder(siteId)`（改：带 siteId）
  - `close_all_video_finders()`（新增）

- [ ] **Step 1: per-site label 辅助函数**

在 `video_finder.rs`：把 `pub(crate) const VIDEO_FINDER_LABEL: &str = "video_finder_webview";` 改为前缀常量 + 函数：

```rust
/// 视频查找窗口 label 前缀（每个源一个窗口，支持并发）
pub(crate) const VIDEO_FINDER_LABEL_PREFIX: &str = "video_finder_";

/// 某源的视频查找窗口 label
pub(crate) fn video_finder_label(site_id: &str) -> String {
    format!("{}{}", VIDEO_FINDER_LABEL_PREFIX, site_id)
}
```

- [ ] **Step 2: link_probe.rs 改用独立 label**

`link_probe.rs` 顶部 `use super::video_finder::{INTERCEPT_JS, VIDEO_FINDER_LABEL};` 改为只引 `INTERCEPT_JS`，并在 link_probe.rs 内定义自己的 label 常量：

```rust
use super::video_finder::INTERCEPT_JS;
const PROBE_LABEL: &str = "video_finder_probe";
```

把文件内所有 `VIDEO_FINDER_LABEL` 引用替换为 `PROBE_LABEL`（`grep -n VIDEO_FINDER_LABEL src/resource_scrape/link_probe.rs` 找全，逐个替换）。

- [ ] **Step 3: open 用 per-site label + 事件打 site 标签**

在 `open_video_finder_webview` 里：
1. 所有 `app.get_webview_window(VIDEO_FINDER_LABEL)` / `WebviewWindowBuilder::new(app, VIDEO_FINDER_LABEL, ...)` / 注入循环里的 `get_webview_window(VIDEO_FINDER_LABEL)` 改成 `video_finder_label(site_id)`（用局部 `let label = video_finder_label(site_id);`）。
2. JS 注入脚本里发事件改成带 site。`INTERCEPT_JS` 里两处 `emit`：
   - `window.__TAURI__.event.emit('video-finder-link', url)` → `emit('video-finder-link', { site: '<SITE>', url: url })`
   - `emit('video-finder-page-state', 'not-found')` → `emit('video-finder-page-state', { site: '<SITE>', state: 'not-found' })`
   做法：把 `INTERCEPT_JS` 里这两处的事件 payload 用占位符 `__VIDEO_FINDER_SITE__`，open 时 `let intercept = INTERCEPT_JS.replace("__VIDEO_FINDER_SITE__", site_id);` 再注入。具体：
   - 在 `INTERCEPT_JS` 顶部加 `var __SITE__ = '__VIDEO_FINDER_SITE__';`
   - `emit('video-finder-link', url)` → `emit('video-finder-link', { site: __SITE__, url: url })`
   - `emit('video-finder-page-state', 'not-found')` → `emit('video-finder-page-state', { site: __SITE__, state: 'not-found' })`
   - `initialization_script(INTERCEPT_JS)` 和 `combined_js` 里用 `&INTERCEPT_JS.replace("__VIDEO_FINDER_SITE__", &site_id_string)`。
3. CF 事件：`webview_support::emit_cf_state(...)` 已带 site_id 参数；确认前端能从 payload 取到 site（若 `emit_cf_state` 的 payload 不含 site，则在 `webview_support` 的对应结构加 `site` 字段并填入——`grep -n "fn emit_cf_state" src/resource_scrape/webview_support.rs` 查看其 payload 结构）。

- [ ] **Step 4: close per-site + close_all**

`close_video_finder_webview` 改签名带 site：

```rust
pub fn close_video_finder_webview(app: &AppHandle, site_id: &str) -> Result<(), String> {
    log::info!("[video_finder] event=close_requested site={}", site_id);
    webview_support::emit_cf_state(app, VIDEO_FINDER_CF_STATE_EVENT, "idle", Some(site_id.to_string()), 0);
    if let Some(window) = app.get_webview_window(&video_finder_label(site_id)) {
        window.close().map_err(|e| format!("关闭窗口失败: {}", e))?;
    }
    Ok(())
}

/// 关闭全部视频查找窗口（停止/卸载时调用）
pub fn close_all_video_finders(app: &AppHandle) {
    let labels: Vec<String> = app
        .webview_windows()
        .keys()
        .filter(|l| l.starts_with(VIDEO_FINDER_LABEL_PREFIX))
        .cloned()
        .collect();
    for label in labels {
        if let Some(w) = app.get_webview_window(&label) {
            let _ = w.close();
        }
    }
    webview_support::emit_cf_state(app, VIDEO_FINDER_CF_STATE_EVENT, "idle", None, 0);
}
```

- [ ] **Step 5: commands.rs 改 command 签名 + 新增 close_all**

`grep -n "video_finder" src/resource_scrape/commands.rs` 找到 open/close 两个 `#[tauri::command]`。
- open 命令（`find_video_links` 之类）：内部调用 `open_video_finder_webview(&app, &code, &site_id, show_on_cf)`，签名已带 site，不变。
- close 命令：改成接收 `site_id: String` 并调 `close_video_finder_webview(&app, &site_id)`。
- 新增：

```rust
#[tauri::command]
pub fn close_all_video_finders(app: tauri::AppHandle) -> Result<(), String> {
    video_finder::close_all_video_finders(&app);
    Ok(())
}
```

- [ ] **Step 6: lib.rs 注册 close_all 命令**

`grep -n "close_video_finder\|find_video_links" src/lib.rs` 找到注册处，在 `invoke_handler` 列表加：`resource_scrape::commands::close_all_video_finders,`（按现有路径前缀对齐）。

- [ ] **Step 7: 后端编译**

Run: `cd src-tauri && cargo check`
Expected: Finished，无错误无警告

- [ ] **Step 8: 提交**

```bash
git add src-tauri/src/resource_scrape/video_finder.rs src-tauri/src/resource_scrape/link_probe.rs src-tauri/src/resource_scrape/commands.rs src-tauri/src/lib.rs
git commit -m "feat: video_finder 支持 per-site 多窗口并行，事件带 siteId

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: 前端 tauri 绑定 + 事件 payload 类型

**Files:**
- Modify: `src/lib/tauri.ts`

本任务**只做加法**，保持构建绿；`closeVideoFinder` 的签名变更（破坏现有调用）放到 Task 6 与组件改造一起做。

**Interfaces:**
- Consumes: Task 3 的事件 payload。
- Produces:
  - `closeAllVideoFinders(): Promise<void>`（新增）
  - 类型 `FinderLinkEvent = { site: string; url: string }`、`FinderPageStateEvent = { site: string; state: string }`、`FinderCfStateEvent = { site: string; status: ...; active: boolean }`
  - 确认 `findVideoLinks(code, siteId)` 现有签名已是带 site（一般无需改）。

- [ ] **Step 1: 加 closeAll 绑定 + 事件类型（不动 closeVideoFinder）**

`grep -n "closeVideoFinder\|findVideoLinks\|video-finder" src/lib/tauri.ts` 确认现有绑定。
- 新增 `export const closeAllVideoFinders = () => invoke<void>('close_all_video_finders')`。
- 导出事件类型：

```ts
export interface FinderLinkEvent { site: string; url: string }
export interface FinderPageStateEvent { site: string; state: string }
export interface FinderCfStateEvent { site: string; status: 'idle' | 'active' | 'passed' | 'timeout' | 'failed'; active: boolean }
```

- [ ] **Step 2: 类型检查（应全绿）**

Run: `bunx vue-tsc --noEmit`
Expected: 无输出（通过）。`closeVideoFinder` 仍是旧签名、现有组件调用不报错。

- [ ] **Step 3: 提交**

```bash
git add src/lib/tauri.ts
git commit -m "feat: 新增 closeAllVideoFinders 绑定与查找事件 payload 类型

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: `useParallelFinder` 调度器 + 纯逻辑单测

调度器把"队列/并发/超时/事件分流/正片判定"封装起来。为可测，把**纯状态机**（取下一个源、settle、是否结束）与**副作用**（invoke open/close、analyzeHls、listen）分离：副作用通过参数注入，单测时传 mock。

**Files:**
- Create: `src/composables/useParallelFinder.ts`
- Test: `src/composables/__tests__/useParallelFinder.test.ts`

**Interfaces:**
- Consumes: `pickRealLink`（Task 1）、`findVideoLinks/closeVideoFinder/closeAllVideoFinders`（Task 4）、现有 `analyzeHls`、`VideoLink`。
- Produces:
  - 类型 `SourceStatus = 'pending' | 'searching' | 'cf' | 'found' | 'failed' | 'notfound'`
  - `SourceState { siteId: string; status: SourceStatus; links: VideoLink[]; realLink: VideoLink | null }`
  - `useParallelFinder(deps)` 返回 `{ sources: Ref<SourceState[]>, running: Ref<boolean>, start(code, siteIds): void, stop(): void, onLink(e), onPageState(e), onCfState(e) }`
  - `deps`（可注入，便于测试）：`{ open(code,site), close(site), closeAll(), analyze(url), concurrency, timeoutSecs, now() }`

- [ ] **Step 1: 写失败测试（纯状态机）**

```ts
// src/composables/__tests__/useParallelFinder.test.ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { createScheduler } from '../useParallelFinder'

// 用假定时器：调度器内部 setTimeout(120s) 不真正挂起，避免测试结束后留真实定时器
beforeEach(() => vi.useFakeTimers())
afterEach(() => vi.useRealTimers())

function deps(over: Partial<any> = {}) {
  return {
    open: vi.fn(async () => {}),
    close: vi.fn(async () => {}),
    closeAll: vi.fn(async () => {}),
    analyze: vi.fn(async () => ({ durationSecs: 3600, height: 1080, isMaster: false, isVod: true, width: 1920 })),
    concurrency: 3,
    timeoutSecs: 120,
    ...over,
  }
}

describe('createScheduler', () => {
  it('start 时只并发开 concurrency 个源', async () => {
    const d = deps({ concurrency: 2 })
    const s = createScheduler(d)
    s.start('ABC-123', ['a', 'b', 'c', 'd'])
    expect(d.open).toHaveBeenCalledTimes(2)
    expect(s.sources.value.filter((x) => x.status === 'searching')).toHaveLength(2)
    expect(s.sources.value.filter((x) => x.status === 'pending')).toHaveLength(2)
  })

  it('某源出正片 → 该源 found、关窗、补开下一个', async () => {
    const d = deps({ concurrency: 1 })
    const s = createScheduler(d)
    s.start('ABC', ['a', 'b'])
    await s.onLink({ site: 'a', url: 'http://x/main.m3u8' }) // analyze 返回 3600s → 正片
    const a = s.sources.value.find((x) => x.siteId === 'a')!
    expect(a.status).toBe('found')
    expect(a.realLink?.url).toBe('http://x/main.m3u8')
    expect(d.close).toHaveBeenCalledWith('a')
    expect(d.open).toHaveBeenCalledWith('ABC', 'b') // 补开下一个
  })

  it('404 且无正片 → notfound、让槽', async () => {
    const d = deps({ concurrency: 1 })
    const s = createScheduler(d)
    s.start('ABC', ['a', 'b'])
    s.onPageState({ site: 'a', state: 'not-found' })
    expect(s.sources.value.find((x) => x.siteId === 'a')!.status).toBe('notfound')
    expect(d.open).toHaveBeenCalledWith('ABC', 'b')
  })

  it('全部源结算后 running=false', async () => {
    const d = deps({ concurrency: 3 })
    const s = createScheduler(d)
    s.start('ABC', ['a'])
    s.onPageState({ site: 'a', state: 'not-found' })
    expect(s.running.value).toBe(false)
  })

  it('stop 关闭全部并清空', async () => {
    const d = deps()
    const s = createScheduler(d)
    s.start('ABC', ['a', 'b', 'c', 'd'])
    s.stop()
    expect(d.closeAll).toHaveBeenCalled()
    expect(s.running.value).toBe(false)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bunx vitest run src/composables/__tests__/useParallelFinder.test.ts`
Expected: FAIL（`createScheduler` 不存在）

- [ ] **Step 3: 写实现**

```ts
// src/composables/useParallelFinder.ts
import { ref, type Ref } from 'vue'
import type { VideoLink } from '@/lib/tauri'
import { pickRealLink } from '@/utils/pickRealLink'

export type SourceStatus = 'pending' | 'searching' | 'cf' | 'found' | 'failed' | 'notfound'

export interface SourceState {
  siteId: string
  status: SourceStatus
  links: VideoLink[]
  realLink: VideoLink | null
}

export interface AnalyzeResult {
  durationSecs?: number; width?: number; height?: number; isMaster?: boolean; isVod?: boolean
}

export interface SchedulerDeps {
  open: (code: string, site: string) => Promise<void>
  close: (site: string) => Promise<void>
  closeAll: () => Promise<void>
  analyze: (url: string) => Promise<AnalyzeResult>
  concurrency: number
  timeoutSecs: number
}

const DONE: SourceStatus[] = ['found', 'failed', 'notfound']

/** 纯调度器（副作用经 deps 注入），便于单测 */
export function createScheduler(deps: SchedulerDeps) {
  const sources = ref<SourceState[]>([]) as Ref<SourceState[]>
  const running = ref(false)
  let code = ''
  const queue: string[] = []
  const seen = new Map<string, Set<string>>() // site → urls
  const timers = new Map<string, ReturnType<typeof setTimeout>>()

  function st(site: string) { return sources.value.find((s) => s.siteId === site) }

  function settle(site: string, status: SourceStatus) {
    const s = st(site)
    if (!s || DONE.includes(s.status)) return
    s.status = status
    const t = timers.get(site); if (t) { clearTimeout(t); timers.delete(site) }
    void deps.close(site)
    pump()
    if (running.value && !sources.value.some((x) => x.status === 'searching' || x.status === 'cf' || x.status === 'pending')) {
      running.value = false
    }
  }

  function launch(site: string) {
    const s = st(site)!
    s.status = 'searching'
    void deps.open(code, site)
    const t = setTimeout(() => settle(site, 'failed'), deps.timeoutSecs * 1000)
    timers.set(site, t)
  }

  function pump() {
    while (sources.value.filter((s) => s.status === 'searching' || s.status === 'cf').length < deps.concurrency && queue.length) {
      launch(queue.shift()!)
    }
  }

  function start(c: string, siteIds: string[]) {
    code = c.trim().toUpperCase()
    running.value = true
    sources.value = siteIds.map((siteId) => ({ siteId, status: 'pending', links: [], realLink: null }))
    seen.clear(); queue.length = 0; queue.push(...siteIds)
    pump()
  }

  function stop() {
    for (const t of timers.values()) clearTimeout(t)
    timers.clear()
    void deps.closeAll()
    running.value = false
    queue.length = 0
  }

  async function onLink(e: { site: string; url: string }) {
    const s = st(e.site)
    if (!s || DONE.includes(s.status)) return
    let set = seen.get(e.site); if (!set) { set = new Set(); seen.set(e.site, set) }
    if (set.has(e.url)) return
    set.add(e.url)
    const lower = e.url.split('?')[0].toLowerCase()
    if (lower.endsWith('.mp4') || lower.endsWith('.ts')) return // 与现有一致：屏蔽 mp4/ts
    const isHls = lower.endsWith('.m3u8') || lower.endsWith('.txt')
    const link: VideoLink = { url: e.url, linkType: isHls ? 'm3u8' : 'auto', isHls, resolution: null }
    s.links = [...s.links, link]
    if (isHls) {
      try {
        const info = await deps.analyze(e.url)
        s.links = s.links.map((l) => (l.url === e.url ? { ...l, ...info, analyzed: true } : l))
      } catch { /* 分析失败：保留链接 */ }
    }
    const real = pickRealLink(s.links)
    if (real) { s.realLink = real; settle(e.site, 'found') }
  }

  function onPageState(e: { site: string; state: string }) {
    const s = st(e.site)
    if (e.state === 'not-found' && s && !s.realLink) settle(e.site, 'notfound')
  }

  function onCfState(e: { site: string; status: string; active: boolean }) {
    const s = st(e.site)
    if (!s || DONE.includes(s.status)) return
    if (e.active) {
      s.status = 'cf'
      const t = timers.get(e.site); if (t) { clearTimeout(t); timers.delete(e.site) } // CF 期间暂停预算
    } else if (s.status === 'cf') {
      s.status = 'searching'
      const t = setTimeout(() => settle(e.site, 'failed'), deps.timeoutSecs * 1000) // 重置满额预算
      timers.set(e.site, t)
    }
  }

  return { sources, running, start, stop, onLink, onPageState, onCfState }
}
```

> 注：组件层（Task 6）再写薄薄一层 `useParallelFinder()`，把真实 `findVideoLinks/closeVideoFinder/closeAllVideoFinders/analyzeHls` 和设置里的 `concurrency/timeout` 包成 `SchedulerDeps` 传给 `createScheduler`，并接 3 个 `listen` 事件到 `onLink/onPageState/onCfState`。

- [ ] **Step 4: 跑测试确认通过**

Run: `bunx vitest run src/composables/__tests__/useParallelFinder.test.ts`
Expected: PASS（5 passed）

- [ ] **Step 5: 提交**

```bash
git add src/composables/useParallelFinder.ts src/composables/__tests__/useParallelFinder.test.ts
git commit -m "feat: 并行查找调度器 createScheduler + 纯逻辑单测

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: VideoLinkFinder.vue UI 改造 + 接调度器

把组件从"单源下拉 + 平铺链接"改成"每源一行 + 并行进度"，接上 Task 5 的调度器。同时把 `closeVideoFinder` 绑定改成带 siteId（Task 4 故意没动它以保持构建绿，这里随组件一起改、并更新唯一调用处）。webview 集成无法自动化，靠 `bun run build` + 手动真机验收。

**Files:**
- Modify: `src/lib/tauri.ts`（`closeVideoFinder` 改为 `(siteId: string) => invoke('close_video_finder', { siteId })`）
- Modify: `src/components/VideoLinkFinder.vue`

**Interfaces:**
- Consumes: `createScheduler`（Task 5）、`pickRealLink`、tauri 绑定（Task 4）、`getVideoSites`、`analyzeHls`、设置 `scrape.linkFinderConcurrency/linkFinderSourceTimeoutSecs`、下载/查重现有逻辑。

- [ ] **Step 1: 组件脚本接调度器**

在 `<script setup>` 里：
- 删除单源相关：`selectedSiteId`、`selectedSiteName`、`pickSite/switchSite/persistSelectedSite`、内联 `realLink`/`handleCapturedUrl`/`analyzeLink`（正片判定改用 `pickRealLink`，链接处理移入调度器）。
- 构造调度器：

```ts
import { createScheduler } from '@/composables/useParallelFinder'
import { findVideoLinks, closeVideoFinder, closeAllVideoFinders, analyzeHls,
  type FinderLinkEvent, type FinderPageStateEvent, type FinderCfStateEvent } from '@/lib/tauri'

const scheduler = createScheduler({
  open: (code, site) => findVideoLinks(code, site),
  close: (site) => closeVideoFinder(site),
  closeAll: () => closeAllVideoFinders(),
  analyze: (url) => analyzeHls(url),
  concurrency: settingsStore.settings.scrape.linkFinderConcurrency ?? 3,
  timeoutSecs: settingsStore.settings.scrape.linkFinderSourceTimeoutSecs ?? 120,
})
const sources = scheduler.sources
const running = scheduler.running
```

- `startFinding()`：取已启用源 id 列表（`sites.value` 全部，顺序即列表序——若 `getVideoSites` 已只返回启用源则直接用，否则按 enabled 过滤），挂 3 个 listen，再 `scheduler.start(code, siteIds)`：

```ts
async function startFinding() {
  const c = code.value.trim().toUpperCase(); if (!c) return
  try { sites.value = await getVideoSites() } catch {}
  await resolveSavePath()
  const siteIds = sites.value.map((s) => s.id)
  unlisten = await listen<FinderLinkEvent>('video-finder-link', (e) => scheduler.onLink(e.payload))
  unlistenPageState = await listen<FinderPageStateEvent>('video-finder-page-state', (e) => scheduler.onPageState(e.payload))
  unlistenCf = await listen<FinderCfStateEvent>('video-finder-cf-state', (e) => scheduler.onCfState(e.payload))
  scheduler.start(c, siteIds)
}
function stopFinding() {
  scheduler.stop()
  if (unlisten) { unlisten(); unlisten = null }
  if (unlistenCf) { unlistenCf(); unlistenCf = null }
  if (unlistenPageState) { unlistenPageState(); unlistenPageState = null }
}
```

- `onUnmounted`：调 `stopFinding()`。
- 进度摘要 computed：

```ts
const total = computed(() => sources.value.length)
const searchedCount = computed(() => sources.value.filter((s) => ['found','failed','notfound'].includes(s.status)).length)
const foundCount = computed(() => sources.value.filter((s) => s.status === 'found').length)
```

- 资源名脱敏（沿用）：`资源 N` 按 sources 索引；开发者模式显示真实名。
- 下载：`handleDownloadSingle(link)` / 批量沿用现有；下载用某源的 `realLink` 或展开里的链接。

- [ ] **Step 2: 模板改为每源一行**

把 `<template>` 顶栏的"网站选择"下拉删掉；把链接列表区换成按 `sources` 渲染：

```vue
<!-- 顶栏：番号输入 + 开始/停止 + 进度 -->
<Button v-if="!running" :disabled="!canStart" @click="startFinding">查找链接</Button>
<Button v-else variant="outline" @click="stopFinding">停止</Button>
<span v-if="total" class="text-xs text-muted-foreground">已搜 {{ searchedCount }}/{{ total }} · 正片 {{ foundCount }}</span>

<!-- 每源一行 -->
<div v-for="(s, i) in sources" :key="s.siteId" class="rounded-md border p-2.5">
  <div class="flex items-center gap-2">
    <span class="text-sm font-medium">{{ isDeveloperMode ? s.siteId : `资源 ${i + 1}` }}</span>
    <Badge v-if="s.status === 'searching'"><Loader2 class="size-3 animate-spin" /> 搜索中</Badge>
    <Badge v-else-if="s.status === 'cf'" variant="secondary">CF 验证中</Badge>
    <Badge v-else-if="s.status === 'found'" class="bg-green-600">✓ 正片</Badge>
    <Badge v-else-if="s.status === 'notfound'" variant="secondary">404</Badge>
    <Badge v-else-if="s.status === 'failed'" variant="secondary">✗ 未找到</Badge>
    <Badge v-else variant="outline">等待中</Badge>
  </div>
  <!-- 找到正片：行内展示 + 操作 -->
  <div v-if="s.realLink" class="mt-1 flex items-center gap-2">
    <Badge v-if="s.realLink.height" variant="outline">{{ s.realLink.height }}p</Badge>
    <Badge v-if="s.realLink.durationSecs" variant="outline">{{ formatDuration(s.realLink.durationSecs) }}</Badge>
    <span class="font-mono text-xs text-muted-foreground break-all flex-1">{{ s.realLink.url }}</span>
    <Button size="icon" variant="ghost" @click="handlePreview(s.realLink)"><Play class="size-4" /></Button>
    <Button size="icon" variant="ghost" @click="handleDownloadSingle(s.realLink)"><Download class="size-4" /></Button>
  </div>
</div>
```

> 「展开看该源其它链接」可作为本任务的可选增强：每行加一个折叠区，渲染 `s.links` 中非正片、非广告/片段的链接（复用现有 `isAdOrClip` 判定）。若时间紧，先只展示正片。

- [ ] **Step 3: 前端构建**

Run: `bun run build`
Expected: `✓ built`，vue-tsc 无类型错误

- [ ] **Step 4: 手动真机验收**

启动 app（开发者另开终端 `bun run tauri dev`，或用 `/run`）：
1. 在「资源链接」搜一个常见番号 → 观察多源**并行**(同时 ≤3 个"搜索中")，离屏窗口不超过 3 个。
2. 某源出正片 → 该源变 `✓ 正片`、补开下一个待搜源。
3. 不存在的番号 → 各源 404/超时后变灰、最终整体停。
4. 点「停止」→ 立即全停、关闭所有查找窗口（任务管理器/日志确认无遗留 `video_finder_*` 窗口）。
5. 对某源正片点下载 → 进下载队列（含查重）。
6. （可选）开「HTTP 失败回退 WebView」后撞 CF 的源 → 弹窗验证、状态 `CF验证中`、验证后继续。

- [ ] **Step 5: 提交**

```bash
git add src/components/VideoLinkFinder.vue
git commit -m "feat: 视频源查找改为多源并行（每源一行进度，每源收一条正片）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 验收（全部任务后）

- `cd src-tauri && cargo check` 通过；`bun run build` 通过。
- `bunx vitest run src/utils src/composables` 全绿。
- 手动：并行 ≤3、每源一正片、404/超时让槽、停止关全部窗口、CF 暂停预算、下载可用。
- 对照 spec §1-§11 逐条已覆盖。
